server:
    port: 8762

spring:
    application:
        name: config-server
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/wenxiLubia/springcloud-config-repository
                    searchPaths: '{application}'
                    username:
                    password:

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
